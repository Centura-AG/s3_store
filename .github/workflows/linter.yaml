name: Run Linter

on:
  pull_request:
  workflow_dispatch:

jobs:
  linter:
    uses: Centura-AG/centura_workflows/.github/workflows/linter.yaml@feature/ci-test-execution
    with:
      app_path: ${{ github.event.repository.name }}
